DATABASE_URL="postgresql://rahulnair@localhost:5432/tvv_travel_os_test?schema=public"

# TripJack connector (Sprint 17) — fake, non-secret test-only values so
# TripJackConfig.isConfigured() is true and the real login/search/details
# code paths are exercised against MSW-mocked HTTP, never a real API.
TRIPJACK_API_URL="https://tripjack-api.example.com"
TRIPJACK_AGENCY_ID="test-agency"
TRIPJACK_USER_ID="test-user"
TRIPJACK_PASSWORD="test-password"
TRIPJACK_TIMEOUT_MS="5000"
TRIPJACK_RETRY_COUNT="2"
