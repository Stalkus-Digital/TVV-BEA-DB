# 03 — Database Architecture

> TODO — not yet populated. Scope: ERD, table list, relationships, indexes, caching strategy. Prior draft material exists in the root-level `DATABASE_SCHEMA.md` and `INVENTORY_SYSTEM.md` (which supersedes parts of the former) and should be reconciled here under the Prisma + PostgreSQL stack decision, not duplicated wholesale. Open gap, not yet resolved: Prisma expects a single schema/database; this file must state how that coexists with the `src/modules/*` bounded-context boundaries approved in `docs/02_SYSTEM_ARCHITECTURE.md` and `CLAUDE.md` (e.g. one logical section per module vs. per-module schema files) — not assumed to default to one shared, module-agnostic schema.

## ERD

TODO

## Table List

TODO

## Relationships

TODO

## Indexes

TODO

## Caching Strategy

TODO
