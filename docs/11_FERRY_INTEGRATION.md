# 11 — Ferry Integration

> TODO — not yet populated. Scope: Ferry operator integration (Makruzz and others) as an adapter behind the Supplier Engine, modeled as `TRANSFER`-kind inventory per `05_INVENTORY_ENGINE.md`. **Before this file is written, resolve the discrepancy noted in `01_CURRENT_SYSTEM_AUDIT.md`: the current repository has no real Ferry API integration — only a static mock UI (`itinerary/ferry-rates/page.tsx`).** Confirm whether a real integration exists elsewhere before scoping this document, since that materially changes whether this is new-build or a wrap-and-adapt job. Folder structure: lives inside `src/modules/supplier/` alongside TripJack, per the Approved Folder Architecture in `CLAUDE.md` — not a top-level module of its own. (This clarification doesn't affect the discrepancy noted above.)

## Operator(s) & API Surface

TODO

## Route/Rate Model

TODO

## Mapping to Inventory (`TRANSFER` kind)

TODO
