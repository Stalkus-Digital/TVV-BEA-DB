# 12 — AI Engine

> TODO — not yet populated. Scope: AI-assisted package generation and content generation as first-class producers of normal records (per the "AI Ready" principle in `00_PROJECT_VISION.md`) — prompt/output tracking, provenance back to Packages, replacing the current `AiStudio.tsx`'s `setTimeout` simulation with a real model call. `DATABASE_SCHEMA.md` (root) sketched the `ai_generation_requests`/`ai_generation_outputs` tables generically; this file should define the actual engine behavior, not just the tables. Folder structure: `src/modules/ai/` per the Approved Folder Architecture in `CLAUDE.md`.

## Generation Request Model

TODO

## Provenance & Review Flow

TODO

## Model/Provider Strategy

TODO
