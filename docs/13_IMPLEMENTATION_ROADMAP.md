# 13 — Implementation Roadmap

> TODO — not yet populated. Scope: the ordered, phased build sequence across all engines above, following the standing governing principles (never delete existing features, reuse existing pages, refactor gradually, preserve backward compatibility, explain before replacing, never rewrite everything at once — see `CLAUDE.md`). A prior phased draft exists in the root-level `ARCHITECTURE_MIGRATION.md` and should inform, not be copy-pasted into, this file once the engine docs above (02–12) are populated. Must explicitly sequence Search Engine and Notification Engine as deferred/future modules (approved in `docs/02_SYSTEM_ARCHITECTURE.md`, not yet scheduled — see `CLAUDE.md`), not silently omit them. Phase sequencing should also reflect that each phase stands up one `src/modules/*` bounded context at a time, per the Approved Folder Architecture.

## Phase Sequence

TODO

## Dependencies Between Phases

TODO

## Risk Mitigation Per Phase

TODO
